function Game(nbEnemy, w, h) {
	if (0 > nbEnemy || nbEnemy > 10 || w < 0 || h < 0) {
		throw new Error("Game");
	} 
	this.nbEnemy = nbEnemy;
	this.getNbEnemy = function() {
		return NbEnemy;
	}
	
	var tab = new 
	this.width = w;
	this.height = h;
	var player = new Player(/*ajout planet*/);
	

}
